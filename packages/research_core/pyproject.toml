[project]
name = "research-core"
version = "0.1.0"
description = "Core Meridian research-loop contracts and utilities."
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.8.0",
  "anthropic>=0.111.0",
  "httpx>=0.25.0,<0.28.0",
  "reportlab>=4.0.0,<5.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
