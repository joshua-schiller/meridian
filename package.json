{
  "name": "meridian",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "typecheck:web": "npm --workspace apps/web run typecheck",
    "validate:fixtures": "PYTHONPATH=packages/research_core python3 -m research_core.validate_fixtures fixtures"
  },
  "overrides": {
    "postcss": "^8.5.15"
  }
}
